express-knex-sequelize
========================

Install dependencies:

```bash
$ npm install -g nodemon
```

```bash
$ npm install
```

  Start the server:

```bash
$ npm start
```

View the website at: http://localhost:3000